/* Deafult Exports: You can only have one default export in file */
const message = () => {
    const name = 'Patrick'
    const age = 45
    return name + ' is ' + age + ' years old'
}

/* export default function saludar() {
    console.log('Hola React!')
} */

export default message
